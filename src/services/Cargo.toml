[workspace]
members = [
    "token-service",
    "model-service",
    "quota-service",
    "memory-service"
]
resolver = "2"

[profile.release]
strip = true
opt-level = "z"  # Optimize for size
lto = true
