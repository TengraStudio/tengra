# IPC API Reference

> Auto-generated by `scripts/generate-ipc-docs.js` — **632 handlers** across **57 modules**.

## Table of Contents

- [Advanced Memory](#advanced-memory) (35)
- [Agent](#agent) (10)
- [Audit](#audit) (2)
- [Auth](#auth) (23)
- [Backup](#backup) (12)
- [Brain](#brain) (8)
- [Chat](#chat) (4)
- [Clipboard](#clipboard) (2)
- [Code Intelligence](#code-intelligence) (18)
- [Code Sandbox](#code-sandbox) (3)
- [Collaboration](#collaboration) (4)
- [Contract](#contract) (1)
- [Db](#db) (28)
- [Debug](#debug) (1)
- [Dialog](#dialog) (2)
- [Export](#export) (2)
- [File Diff](#file-diff) (8)
- [Files](#files) (15)
- [Gallery](#gallery) (5)
- [Git Advanced](#git-advanced) (33)
- [Git](#git) (18)
- [Health](#health) (4)
- [Huggingface](#huggingface) (31)
- [Idea Generator](#idea-generator) (28)
- [Key Rotation](#key-rotation) (4)
- [Lazy Services](#lazy-services) (1)
- [Llama](#llama) (11)
- [Logging](#logging) (4)
- [Marketplace](#marketplace) (4)
- [Mcp Marketplace](#mcp-marketplace) (24)
- [Mcp](#mcp) (9)
- [Memory](#memory) (6)
- [Metrics](#metrics) (3)
- [Migration](#migration) (1)
- [Model Downloader](#model-downloader) (4)
- [Model Registry](#model-registry) (3)
- [Multi Model](#multi-model) (1)
- [Ollama](#ollama) (25)
- [Orchestrator](#orchestrator) (4)
- [Performance](#performance) (4)
- [Process](#process) (6)
- [Project Agent](#project-agent) (88)
- [Project](#project) (14)
- [Prompt Templates](#prompt-templates) (11)
- [Proxy Embed](#proxy-embed) (3)
- [Proxy](#proxy) (16)
- [Screenshot](#screenshot) (1)
- [Sd Cpp](#sd-cpp) (28)
- [Settings](#settings) (3)
- [Theme](#theme) (24)
- [Token Estimation](#token-estimation) (9)
- [Tools](#tools) (3)
- [Usage](#usage) (3)
- [User Behavior](#user-behavior) (3)
- [User Collaboration](#user-collaboration) (3)
- [Window](#window) (5)
- [Workflow](#workflow) (7)

---

## Advanced Memory

_Source: `src/main/ipc/advanced-memory.ts`_

| Channel | Description |
|---------|-------------|
| `advancedMemory:getPending` | — |
| `advancedMemory:confirm` | — |
| `advancedMemory:reject` | — |
| `advancedMemory:confirmAll` | — |
| `advancedMemory:rejectAll` | — |
| `advancedMemory:remember` | — |
| `advancedMemory:recall` | — |
| `advancedMemory:search` | — |
| `advancedMemory:getSearchAnalytics` | — |
| `advancedMemory:getSearchHistory` | — |
| `advancedMemory:getSearchSuggestions` | — |
| `advancedMemory:export` | — |
| `advancedMemory:import` | — |
| `advancedMemory:getStats` | — |
| `advancedMemory:runDecay` | — |
| `advancedMemory:recategorize` | — |
| `advancedMemory:extractFromMessage` | — |
| `advancedMemory:delete` | — |
| `advancedMemory:deleteMany` | — |
| `advancedMemory:edit` | — |
| `advancedMemory:archive` | — |
| `advancedMemory:archiveMany` | — |
| `advancedMemory:restore` | — |
| `advancedMemory:get` | — |
| `advancedMemory:shareWithProject` | — |
| `advancedMemory:createSharedNamespace` | — |
| `advancedMemory:syncSharedNamespace` | — |
| `advancedMemory:getSharedNamespaceAnalytics` | — |
| `advancedMemory:searchAcrossProjects` | — |
| `advancedMemory:getHistory` | — |
| `advancedMemory:rollback` | — |
| `advancedMemory:getAllEntityKnowledge` | — |
| `advancedMemory:getAllEpisodes` | — |
| `advancedMemory:getAllAdvancedMemories` | — |
| `advancedMemory:health` | — |

---

## Agent

_Source: `src/main/ipc/agent.ts`_

| Channel | Description |
|---------|-------------|
| `agent:get-all` | IPC handlers for Agent Service/ import { AgentService } from '@main/services/llm/agent.service'; import { createMainWindowSenderValidator } from '@main/ipc/sender-validator'; import { createValidatedIpcHandler } from '@main/utils/ipc-wrapper.util'; import { BrowserWindow, ipcMain } from 'electron'; import { z } from 'zod';  const agentSchema = z.object({     id: z.string().optional(),     name: z.string().optional(),     description: z.string().optional(),     systemPrompt: z.string().optional(),     tools: z.array(z.string()).optional(),     parentModel: z.string().optional(),     color: z.string().optional() });  const createAgentPayloadSchema = z.object({     agent: agentSchema.optional(),     options: z.object({         cloneFromId: z.string().optional(),         createWorkspace: z.boolean().optional()     }).optional() });  const deleteAgentOptionsSchema = z.object({     confirm: z.boolean().optional(),     softDelete: z.boolean().optional(),     backupBeforeDelete: z.boolean().optional() }).optional();  const templateSchema = z.object({     name: z.string().optional(),     description: z.string().optional(),     systemPrompt: z.string().optional(),     tools: z.array(z.string()).optional(),     parentModel: z.string().optional(),     color: z.string().optional() });  type CreateAgentPayload = z.infer<typeof createAgentPayloadSchema>; type DeleteAgentOptions = z.infer<typeof deleteAgentOptionsSchema>; type Template = z.infer<typeof templateSchema>;  export function registerAgentIpc(getMainWindow: () => BrowserWindow \| null, agentService: AgentService) {     const validateSender = createMainWindowSenderValidator(getMainWindow, 'agent operation');      /**Get all agentsReturns an empty array on failure |
| `agent:get` | Get a specific agent by IDReturns null on failure |
| `agent:create` | — |
| `agent:delete` | — |
| `agent:clone` | — |
| `agent:export` | — |
| `agent:import` | — |
| `agent:get-templates-library` | — |
| `agent:validate-template` | — |
| `agent:recover` | — |

---

## Audit

_Source: `src/main/ipc/audit.ts`_

| Channel | Description |
|---------|-------------|
| `audit:getLogs` | Registers IPC handlers for audit log operations.Handles retrieving and clearing audit logs.@param auditLogService - Service for interacting with audit logs/ export function registerAuditIpc(auditLogService: AuditLogService) {     /**Retrieves audit logs with optional filtering.@param options - Optional filtering criteria@param options.category - Filter by audit category@param options.startDate - Filter logs after this timestamp (ms)@param options.endDate - Filter logs before this timestamp (ms)@param options.limit - Maximum number of logs to return |
| `audit:clearLogs` | Clears all audit logs. |

---

## Auth

_Source: `src/main/ipc/auth.ts`_

| Channel | Description |
|---------|-------------|
| `auth:github-login` | — |
| `auth:poll-token` | — |
| `auth:set-active-linked-account` | — |
| `auth:link-account` | — |
| `auth:unlink-account` | — |
| `auth:unlink-provider` | — |
| `auth:detect-provider` | — |
| `auth:get-provider-health` | — |
| `auth:get-provider-analytics` | — |
| `auth:rotate-token-encryption` | — |
| `auth:revoke-account-token` | — |
| `auth:get-token-analytics` | — |
| `auth:export-credentials` | — |
| `auth:import-credentials` | — |
| `auth:create-master-key-backup` | — |
| `auth:restore-master-key-backup` | — |
| `auth:start-session` | — |
| `auth:touch-session` | — |
| `auth:end-session` | — |
| `auth:set-session-limit` | — |
| `auth:get-session-analytics` | — |
| `auth:set-session-timeout` | — |
| `auth:get-session-timeout` | — |

---

## Backup

_Source: `src/main/ipc/backup.ts`_

| Channel | Description |
|---------|-------------|
| `backup:create` | — |
| `backup:restore` | — |
| `backup:list` | — |
| `backup:delete` | — |
| `backup:getDir` | — |
| `backup:getAutoBackupStatus` | — |
| `backup:configureAutoBackup` | — |
| `backup:cleanup` | — |
| `backup:verify` | — |
| `backup:syncToCloudDir` | — |
| `backup:createDisasterRecoveryBundle` | — |
| `backup:restoreDisasterRecoveryBundle` | — |

---

## Brain

_Source: `src/main/ipc/brain.ts`_

| Channel | Description |
|---------|-------------|
| `brain:learn` | — |
| `brain:recall` | — |
| `brain:getByCategory` | — |
| `brain:getContext` | — |
| `brain:extractFromMessage` | — |
| `brain:forget` | — |
| `brain:updateConfidence` | — |
| `brain:getStats` | — |

---

## Chat

_Source: `src/main/ipc/chat.ts`_

| Channel | Description |
|---------|-------------|
| `chat:openai` | — |
| `chat:stream` | — |
| `chat:copilot` | — |
| `chat:retry-with-model` | — |

---

## Clipboard

_Source: `src/main/ipc/clipboard.ts`_

| Channel | Description |
|---------|-------------|
| `clipboard:writeText` | — |
| `clipboard:readText` | — |

---

## Code Intelligence

_Source: `src/main/ipc/code-intelligence.ts`_

| Channel | Description |
|---------|-------------|
| `code:findSymbols` | Registers IPC handlers for code intelligence operations./ export function registerCodeIntelligenceIpc(codeIntelligenceService: CodeIntelligenceService) {     /**Find symbols by name/query |
| `code:searchFiles` | Search files in project |
| `code:indexProject` | Index a project |
| `code:querySymbols` | Query indexed symbols semantically |
| `code:getFileOutline` | Get outline for a file |
| `code:findDefinition` | Find definition for a symbol |
| `code:findUsage` | Find usage for a symbol |
| `code:findReferences` | Find references for a symbol |
| `code:findImplementations` | Find implementations for a symbol |
| `code:getSymbolRelationships` | Get related files/symbols for a given symbol |
| `code:queryIndexedSymbols` | Query indexed symbols semantically |
| `code:getSymbolAnalytics` | Get symbol analytics for a project |
| `code:scanTodos` | Scan for TODOs in a project |
| `code:analyzeQuality` | Get code quality analysis |
| `code:previewRenameSymbol` | Preview symbol rename |
| `code:applyRenameSymbol` | Apply symbol rename |
| `code:generateFileDocumentation` | Generate file documentation |
| `code:generateProjectDocumentation` | Generate project documentation |

---

## Code Sandbox

_Source: `src/main/ipc/code-sandbox.ts`_

| Channel | Description |
|---------|-------------|
| `code-sandbox:languages` | — |
| `code-sandbox:execute` | — |
| `code-sandbox:health` | — |

---

## Collaboration

_Source: `src/main/ipc/collaboration.ts`_

| Channel | Description |
|---------|-------------|
| `collaboration:run` | IPC Handlers for Model Collaboration/  import { createMainWindowSenderValidator } from '@main/ipc/sender-validator'; import { ModelCollaborationService } from '@main/services/llm/model-collaboration.service'; import { multiLLMOrchestrator } from '@main/services/llm/multi-llm-orchestrator.service'; import { createIpcHandler, createSafeIpcHandler } from '@main/utils/ipc-wrapper.util'; import { Message } from '@shared/types/chat'; import { BrowserWindow, ipcMain, IpcMainInvokeEvent } from 'electron';  /**Registers IPC handlers for model collaboration@param collaborationService Service for model collaboration/ export function registerCollaborationIpc(getMainWindow: () => BrowserWindow \| null, collaborationService: ModelCollaborationService) {     const validateSender = createMainWindowSenderValidator(getMainWindow, 'collaboration operation');      /**Run multiple models in collaboration |
| `collaboration:getProviderStats` | Get provider statistics |
| `collaboration:getActiveTaskCount` | Get active task count for a provider |
| `collaboration:setProviderConfig` | Configure provider settings |

---

## Contract

_Source: `src/main/ipc/contract.ts`_

| Channel | Description |
|---------|-------------|
| `ipc:contract:get` | — |

---

## Db

_Source: `src/main/ipc/db.ts`_

| Channel | Description |
|---------|-------------|
| `db:createChat` | — |
| `db:pinChat` | — |
| `db:favoriteChat` | — |
| `db:archiveChat` | — |
| `db:updateChatTitle` | — |
| `db:deleteMessages` | — |
| `db:searchChats` | — |
| `db:clearHistory` | — |
| `db:createProject` | — |
| `db:getProjects` | — |
| `db:getProjectById` | — |
| `db:updateProject` | — |
| `db:deleteProject` | — |
| `db:createFolder` | — |
| `db:getFolders` | — |
| `db:updateFolder` | — |
| `db:deleteFolder` | — |
| `db:moveChatToFolder` | — |
| `db:recordUsage` | — |
| `db:getUsageStats` | — |
| `db:searchSimilarMessages` | — |
| `db:updateMessageVector` | — |
| `db:createPrompt` | — |
| `db:deletePrompt` | — |
| `db:updatePrompt` | — |
| `db:getPrompts` | — |
| `db:getDetailedStats` | — |
| `db:getProviderStats` | — |

---

## Debug

_Source: `src/main/ipc/debug.ts`_

| Channel | Description |
|---------|-------------|
| `debug:dependency-graph` | — |

---

## Dialog

_Source: `src/main/ipc/dialog.ts`_

| Channel | Description |
|---------|-------------|
| `dialog:selectDirectory` | — |
| `dialog:saveFile` | — |

---

## Export

_Source: `src/main/ipc/export.ts`_

| Channel | Description |
|---------|-------------|
| `export:markdown` | — |
| `export:pdf` | — |

---

## File Diff

_Source: `src/main/ipc/file-diff.ts`_

| Channel | Description |
|---------|-------------|
| `diff:getFileHistory` | — |
| `diff:getRecentChanges` | — |
| `diff:getSessionChanges` | — |
| `diff:getChangesBySystem` | — |
| `diff:getDiffById` | — |
| `diff:revertChange` | — |
| `diff:getStats` | — |
| `diff:cleanup` | — |

---

## Files

_Source: `src/main/ipc/files.ts`_

| Channel | Description |
|---------|-------------|
| `files:exists` | — |
| `files:selectDirectory` | — |
| `files:selectFile` | — |
| `files:listDirectory` | — |
| `files:readFile` | — |
| `files:readImage` | — |
| `files:writeFile` | — |
| `files:createDirectory` | — |
| `files:deleteFile` | — |
| `files:deleteDirectory` | — |
| `files:renamePath` | — |
| `files:searchFiles` | — |
| `files:health` | — |
| `files:searchFilesStream` | — |
| `app:getUserDataPath` | — |

---

## Gallery

_Source: `src/main/ipc/gallery.ts`_

| Channel | Description |
|---------|-------------|
| `gallery:list` | — |
| `gallery:delete` | — |
| `gallery:open` | — |
| `gallery:reveal` | — |
| `gallery:batch-download` | — |

---

## Git Advanced

_Source: `src/main/ipc/git-advanced.ts`_

| Channel | Description |
|---------|-------------|
| `git:getConflicts` | — |
| `git:resolveConflict` | — |
| `git:openMergeTool` | — |
| `git:getStashes` | — |
| `git:createStash` | — |
| `git:applyStash` | — |
| `git:dropStash` | — |
| `git:exportStash` | — |
| `git:getBlame` | — |
| `git:getCommitDetails` | — |
| `git:getRebaseStatus` | — |
| `git:getRebasePlan` | — |
| `git:startRebase` | — |
| `git:continueRebase` | — |
| `git:abortRebase` | — |
| `git:getSubmodules` | — |
| `git:initSubmodules` | — |
| `git:updateSubmodules` | — |
| `git:syncSubmodules` | — |
| `git:addSubmodule` | — |
| `git:removeSubmodule` | — |
| `git:getFlowStatus` | — |
| `git:startFlowBranch` | — |
| `git:finishFlowBranch` | — |
| `git:getHooks` | — |
| `git:installHook` | — |
| `git:validateHook` | — |
| `git:testHook` | — |
| `git:exportHooks` | — |
| `git:getRepositoryStats` | — |
| `git:exportRepositoryStats` | — |
| `git:runControlledOperation` | — |
| `git:cancelOperation` | — |

---

## Git

_Source: `src/main/ipc/git.ts`_

| Channel | Description |
|---------|-------------|
| `git:isRepository` | — |
| `git:getTreeStatus` | — |
| `git:getRemotes` | — |
| `git:getTrackingInfo` | — |
| `git:getLastCommit` | — |
| `git:getRecentCommits` | — |
| `git:getCommitStats` | — |
| `git:getFileDiff` | — |
| `git:getUnifiedDiff` | — |
| `git:getCommitDiff` | — |
| `git:stageFile` | — |
| `git:unstageFile` | — |
| `git:getDetailedStatus` | — |
| `git:getDiffStats` | — |
| `git:checkout` | — |
| `git:commit` | — |
| `git:push` | — |
| `git:pull` | — |

---

## Health

_Source: `src/main/ipc/health.ts`_

| Channel | Description |
|---------|-------------|
| `health:status` | Registers IPC handlers for health check endpoints/ export function registerHealthIpc(healthCheckService: HealthCheckService) {     /**Get overall health status of all registered services |
| `health:check` | Check a specific service immediately |
| `health:getService` | Get health status for a specific service |
| `health:listServices` | List all registered service names |

---

## Huggingface

_Source: `src/main/ipc/huggingface.ts`_

| Channel | Description |
|---------|-------------|
| `hf:search-models` | — |
| `hf:get-files` | — |
| `hf:download-file` | — |
| `hf:cancel-download` | — |
| `hf:get-recommendations` | — |
| `hf:get-model-preview` | — |
| `hf:compare-models` | — |
| `hf:validate-compatibility` | — |
| `hf:watchlist:get` | — |
| `hf:watchlist:add` | — |
| `hf:watchlist:remove` | — |
| `hf:cache-stats` | — |
| `hf:cache-clear` | — |
| `hf:test-downloaded-model` | — |
| `hf:get-conversion-presets` | — |
| `hf:get-optimization-suggestions` | — |
| `hf:validate-conversion` | — |
| `hf:convert-model` | — |
| `hf:versions:list` | — |
| `hf:versions:register` | — |
| `hf:versions:compare` | — |
| `hf:versions:rollback` | — |
| `hf:versions:pin` | — |
| `hf:versions:notifications` | — |
| `hf:finetune:prepare-dataset` | — |
| `hf:finetune:start` | — |
| `hf:finetune:list` | — |
| `hf:finetune:get` | — |
| `hf:finetune:cancel` | — |
| `hf:finetune:evaluate` | — |
| `hf:finetune:export` | — |

---

## Idea Generator

_Source: `src/main/ipc/idea-generator.ts`_

| Channel | Description |
|---------|-------------|
| `ideas:createSession` | — |
| `ideas:getSession` | — |
| `ideas:getSessions` | — |
| `ideas:cancelSession` | — |
| `ideas:generateMarketPreview` | — |
| `ideas:startResearch` | — |
| `ideas:startGeneration` | — |
| `ideas:enrichIdea` | — |
| `ideas:getIdea` | — |
| `ideas:getIdeas` | — |
| `ideas:regenerateIdea` | — |
| `ideas:approveIdea` | — |
| `ideas:rejectIdea` | — |
| `ideas:canGenerateLogo` | — |
| `ideas:generateLogo` | — |
| `ideas:deepResearch` | — |
| `ideas:validateIdea` | — |
| `ideas:clearResearchCache` | — |
| `ideas:scoreIdea` | — |
| `ideas:rankIdeas` | — |
| `ideas:compareIdeas` | — |
| `ideas:quickScore` | — |
| `ideas:deleteIdea` | — |
| `ideas:deleteSession` | — |
| `ideas:archiveIdea` | — |
| `ideas:restoreIdea` | — |
| `ideas:getArchivedIdeas` | — |
| `ideas:queryResearch` | — |

---

## Key Rotation

_Source: `src/main/ipc/key-rotation.ts`_

| Channel | Description |
|---------|-------------|
| `key-rotation:getCurrentKey` | Registers IPC handlers for API key rotation management/ export function registerKeyRotationIpc(getMainWindow: () => BrowserWindow \| null, keyRotationService: KeyRotationService) {     const validateSender = createMainWindowSenderValidator(getMainWindow, 'key-rotation operation');     appLogger.info('KeyRotationIPC', 'Registering key rotation IPC handlers');      /**Get current key for a provider |
| `key-rotation:rotate` | Rotate to the next key for a provider |
| `key-rotation:initialize` | Initialize provider keys (comma-separated) |
| `key-rotation:getStatus` | Get rotation status for a provider |

---

## Lazy Services

_Source: `src/main/ipc/lazy-services.ts`_

| Channel | Description |
|---------|-------------|
| `lazy:get-status` | — |

---

## Llama

_Source: `src/main/ipc/llama.ts`_

| Channel | Description |
|---------|-------------|
| `llama:loadModel` | — |
| `llama:unloadModel` | — |
| `llama:chat` | — |
| `llama:resetSession` | — |
| `llama:getModels` | — |
| `llama:downloadModel` | — |
| `llama:deleteModel` | — |
| `llama:getConfig` | — |
| `llama:setConfig` | — |
| `llama:getGpuInfo` | — |
| `llama:getModelsDir` | — |

---

## Logging

_Source: `src/main/ipc/logging.ts`_

| Channel | Description |
|---------|-------------|
| `log:stream:start` | — |
| `log:stream:stop` | — |
| `log:buffer:get` | — |
| `log:buffer:clear` | — |

---

## Marketplace

_Source: `src/main/ipc/marketplace.ts`_

| Channel | Description |
|---------|-------------|
| `marketplace:getModels` | — |
| `marketplace:searchModels` | — |
| `marketplace:getModelDetails` | — |
| `marketplace:getStatus` | — |

---

## Mcp Marketplace

_Source: `src/main/ipc/mcp-marketplace.ts`_

| Channel | Description |
|---------|-------------|
| `mcp:marketplace:list` | — |
| `mcp:marketplace:search` | — |
| `mcp:marketplace:filter` | — |
| `mcp:marketplace:categories` | — |
| `mcp:marketplace:extension-templates` | — |
| `mcp:marketplace:draft-extension` | — |
| `mcp:marketplace:install` | — |
| `mcp:marketplace:uninstall` | — |
| `mcp:marketplace:installed` | — |
| `mcp:marketplace:toggle` | — |
| `mcp:marketplace:refresh` | — |
| `mcp:marketplace:update-config` | — |
| `mcp:marketplace:version-history` | — |
| `mcp:marketplace:rollback-version` | — |
| `mcp:marketplace:security-scan` | — |
| `mcp:marketplace:reviews:list` | — |
| `mcp:marketplace:reviews:submit` | — |
| `mcp:marketplace:reviews:moderate` | — |
| `mcp:marketplace:reviews:vote` | — |
| `mcp:marketplace:telemetry:track` | — |
| `mcp:marketplace:telemetry:crash` | — |
| `mcp:marketplace:telemetry:summary` | — |
| `mcp:marketplace:debug` | — |
| `mcp:marketplace:health` | — |

---

## Mcp

_Source: `src/main/ipc/mcp.ts`_

| Channel | Description |
|---------|-------------|
| `mcp:list` | — |
| `mcp:dispatch` | — |
| `mcp:toggle` | — |
| `mcp:install` | — |
| `mcp:uninstall` | — |
| `mcp:debug-metrics` | — |
| `mcp:permissions:list-requests` | — |
| `mcp:permissions:set` | — |
| `mcp:permissions:resolve-request` | — |

---

## Memory

_Source: `src/main/ipc/memory.ts`_

| Channel | Description |
|---------|-------------|
| `memory:getAll` | — |
| `memory:deleteFact` | — |
| `memory:deleteEntity` | — |
| `memory:addFact` | — |
| `memory:setEntityFact` | — |
| `memory:search` | — |

---

## Metrics

_Source: `src/main/ipc/metrics.ts`_

| Channel | Description |
|---------|-------------|
| `metrics:get-provider-stats` | — |
| `metrics:get-summary` | — |
| `metrics:reset` | — |

---

## Migration

_Source: `src/main/ipc/migration.ts`_

| Channel | Description |
|---------|-------------|
| `migration:status` | Registers IPC handlers for database migration management/ export function registerMigrationIpc(databaseService: DatabaseService) {     appLogger.info('MigrationIPC', 'Registering migration IPC handlers');      /**Get migration status |

---

## Model Downloader

_Source: `src/main/ipc/model-downloader.ts`_

| Channel | Description |
|---------|-------------|
| `model-downloader:start` | — |
| `model-downloader:pause` | — |
| `model-downloader:cancel` | — |
| `model-downloader:resume` | — |

---

## Model Registry

_Source: `src/main/ipc/model-registry.ts`_

| Channel | Description |
|---------|-------------|
| `model-registry:getAllModels` | — |
| `model-registry:getRemoteModels` | — |
| `model-registry:getInstalledModels` | — |

---

## Multi Model

_Source: `src/main/ipc/multi-model.ts`_

| Channel | Description |
|---------|-------------|
| `llm:compare-models` | — |

---

## Ollama

_Source: `src/main/ipc/ollama.ts`_

| Channel | Description |
|---------|-------------|
| `ollama:tags` | — |
| `ollama:getModels` | — |
| `ollama:isRunning` | — |
| `ollama:healthStatus` | — |
| `ollama:forceHealthCheck` | — |
| `ollama:checkCuda` | — |
| `ollama:chat` | — |
| `ollama:chatStream` | — |
| `ollama:abort` | — |
| `ollama:pull` | — |
| `ollama:abortPull` | — |
| `ollama:getLibraryModels` | — |
| `ollama:start` | — |
| `ollama:checkModelHealth` | — |
| `ollama:checkAllModelsHealth` | — |
| `ollama:getModelRecommendations` | — |
| `ollama:getRecommendedModelForTask` | — |
| `ollama:getConnectionStatus` | — |
| `ollama:testConnection` | — |
| `ollama:reconnect` | — |
| `ollama:getGPUInfo` | — |
| `ollama:startGPUMonitoring` | — |
| `ollama:stopGPUMonitoring` | — |
| `ollama:setGPUAlertThresholds` | — |
| `ollama:getGPUAlertThresholds` | — |

---

## Orchestrator

_Source: `src/main/ipc/orchestrator.ts`_

| Channel | Description |
|---------|-------------|
| `orchestrator:start` | — |
| `orchestrator:approve` | — |
| `orchestrator:get-state` | — |
| `orchestrator:stop` | — |

---

## Performance

_Source: `src/main/ipc/performance.ts`_

| Channel | Description |
|---------|-------------|
| `performance:get-memory-stats` | — |
| `performance:detect-leak` | — |
| `performance:trigger-gc` | — |
| `performance:get-dashboard` | — |

---

## Process

_Source: `src/main/ipc/process.ts`_

| Channel | Description |
|---------|-------------|
| `process:spawn` | — |
| `process:kill` | — |
| `process:list` | — |
| `process:scan-scripts` | — |
| `process:resize` | — |
| `process:write` | — |

---

## Project Agent

_Source: `src/main/ipc/project-agent.ts`_

| Channel | Description |
|---------|-------------|
| `project:start` | — |
| `project:stop` | — |
| `project:pause-task` | — |
| `project:resume-task` | — |
| `project:save-snapshot` | — |
| `project:reset-state` | — |
| `project:plan` | — |
| `project:approve` | — |
| `project:approve-current-plan` | — |
| `project:reject-current-plan` | — |
| `project:get-status` | — |
| `project:get-messages` | — |
| `project:get-events` | — |
| `project:council-generate-plan` | — |
| `project:council-get-proposal` | — |
| `project:council-approve-proposal` | — |
| `project:council-reject-proposal` | — |
| `project:council-start-execution` | — |
| `project:council-pause-execution` | — |
| `project:council-resume-execution` | — |
| `project:council-get-timeline` | — |
| `project:get-telemetry` | — |
| `project:get-task-history` | — |
| `project:delete-task` | — |
| `project:get-available-models` | — |
| `project:retry-step` | — |
| `project:select-model` | — |
| `project:approve-step` | — |
| `project:skip-step` | — |
| `project:edit-step` | — |
| `project:add-step-comment` | — |
| `project:insert-intervention` | — |
| `project:resume-checkpoint` | — |
| `project:get-checkpoints` | — |
| `project:rollback-checkpoint` | — |
| `project:get-plan-versions` | — |
| `project:delete-task-by-node` | — |
| `project:create-pr` | — |
| `project:get-profiles` | — |
| `project:register-profile` | — |
| `project:delete-profile` | — |
| `project:get-routing-rules` | — |
| `project:set-routing-rules` | — |
| `project:create-voting-session` | — |
| `project:submit-vote` | — |
| `project:request-votes` | — |
| `project:resolve-voting` | — |
| `project:get-voting-session` | — |
| `project:list-voting-sessions` | — |
| `project:override-voting` | — |
| `project:get-voting-analytics` | — |
| `project:get-voting-config` | — |
| `project:update-voting-config` | — |
| `project:list-voting-templates` | — |
| `project:build-consensus` | — |
| `project:create-debate-session` | — |
| `project:submit-debate-argument` | — |
| `project:resolve-debate-session` | — |
| `project:override-debate-session` | — |
| `project:get-debate-session` | — |
| `project:list-debate-history` | — |
| `project:get-debate-replay` | — |
| `project:generate-debate-summary` | — |
| `project:get-teamwork-analytics` | — |
| `project:get-performance-metrics` | — |
| `project:health` | — |
| `project:council-send-message` | — |
| `project:council-get-messages` | — |
| `project:council-cleanup-expired-messages` | — |
| `project:council-handle-quota-interrupt` | — |
| `project:council-register-worker-availability` | — |
| `project:council-list-available-workers` | — |
| `project:council-score-helper-candidates` | — |
| `project:council-generate-helper-handoff` | — |
| `project:council-review-helper-merge` | — |
| `project:save-canvas-nodes` | — |
| `project:get-canvas-nodes` | — |
| `project:delete-canvas-node` | — |
| `project:save-canvas-edges` | — |
| `project:get-canvas-edges` | — |
| `project:delete-canvas-edge` | — |
| `project:get-templates` | — |
| `project:save-template` | — |
| `project:delete-template` | — |
| `project:export-template` | — |
| `project:import-template` | — |
| `project:apply-template` | — |
| `project:get-template` | — |

---

## Project

_Source: `src/main/ipc/project.ts`_

| Channel | Description |
|---------|-------------|
| `project:analyze` | — |
| `project:watch` | — |
| `project:unwatch` | — |
| `project:generateLogo` | — |
| `project:analyzeIdentity` | — |
| `project:analyzeDirectory` | — |
| `project:applyLogo` | — |
| `project:getCompletion` | — |
| `project:getInlineSuggestion` | — |
| `project:trackInlineSuggestionTelemetry` | — |
| `project:improveLogoPrompt` | — |
| `project:uploadLogo` | — |
| `project:getEnv` | — |
| `project:saveEnv` | — |

---

## Prompt Templates

_Source: `src/main/ipc/prompt-templates.ts`_

| Channel | Description |
|---------|-------------|
| `prompt-templates:getAll` | Registers IPC handlers for prompt templates library/ export function registerPromptTemplatesIpc(getMainWindow: () => BrowserWindow \| null, promptTemplatesService: PromptTemplatesService) {     const validateSender = createMainWindowSenderValidator(getMainWindow, 'prompt-templates operation');     /**Get all templates (builtin + custom) |
| `prompt-templates:getByCategory` | Get templates by category |
| `prompt-templates:getByTag` | Get templates by tag |
| `prompt-templates:search` | Search templates |
| `prompt-templates:get` | Get a template by ID |
| `prompt-templates:create` | Create a new custom template |
| `prompt-templates:update` | Update an existing custom template |
| `prompt-templates:delete` | Delete a custom template |
| `prompt-templates:render` | Render a template with variables |
| `prompt-templates:getCategories` | Get all categories |
| `prompt-templates:getTags` | Get all tags |

---

## Proxy Embed

_Source: `src/main/ipc/proxy-embed.ts`_

| Channel | Description |
|---------|-------------|
| `proxy:embed:start` | — |
| `proxy:embed:stop` | — |
| `proxy:embed:status` | — |

---

## Proxy

_Source: `src/main/ipc/proxy.ts`_

| Channel | Description |
|---------|-------------|
| `proxy:antigravityLogin` | — |
| `proxy:claudeLogin` | — |
| `proxy:saveClaudeSession` | — |
| `proxy:anthropicLogin` | — |
| `proxy:codexLogin` | — |
| `proxy:getModels` | — |
| `proxy:getQuota` | — |
| `proxy:getCopilotQuota` | — |
| `proxy:getCodexUsage` | — |
| `proxy:getClaudeQuota` | — |
| `proxy:get-rate-limit-metrics` | — |
| `proxy:get-rate-limit-config` | — |
| `proxy:set-rate-limit-config` | — |
| `proxy:deleteAuthFile` | — |
| `proxy:syncAuthFiles` | — |
| `proxy:downloadAuthFile` | — |

---

## Screenshot

_Source: `src/main/ipc/screenshot.ts`_

| Channel | Description |
|---------|-------------|
| `screenshot:capture` | — |

---

## Sd Cpp

_Source: `src/main/ipc/sd-cpp.ts`_

| Channel | Description |
|---------|-------------|
| `sd-cpp:getStatus` | IPC handlers for SD-CPP (Stable Diffusion C++) service/ import { LocalImageService } from '@main/services/llm/local-image.service'; import { createSafeIpcHandler } from '@main/utils/ipc-wrapper.util'; import { ipcMain } from 'electron';  /**Registers SD-CPP related IPC handlers.@param localImageService - The LocalImageService instance to use./ export const registerSdCppIpc = (localImageService: LocalImageService) => {     /**Get the current status of the SD-CPP runtime |
| `sd-cpp:reinstall` | Force a reinstallation / repair of the SD-CPP runtime |
| `sd-cpp:getHistory` | — |
| `sd-cpp:regenerate` | — |
| `sd-cpp:getAnalytics` | — |
| `sd-cpp:getPresetAnalytics` | — |
| `sd-cpp:getScheduleAnalytics` | — |
| `sd-cpp:listPresets` | — |
| `sd-cpp:savePreset` | — |
| `sd-cpp:deletePreset` | — |
| `sd-cpp:exportPresetShare` | — |
| `sd-cpp:importPresetShare` | — |
| `sd-cpp:listWorkflowTemplates` | — |
| `sd-cpp:saveWorkflowTemplate` | — |
| `sd-cpp:deleteWorkflowTemplate` | — |
| `sd-cpp:exportWorkflowTemplateShare` | — |
| `sd-cpp:importWorkflowTemplateShare` | — |
| `sd-cpp:schedule` | — |
| `sd-cpp:listSchedules` | — |
| `sd-cpp:cancelSchedule` | — |
| `sd-cpp:compare` | — |
| `sd-cpp:exportComparison` | — |
| `sd-cpp:shareComparison` | — |
| `sd-cpp:batchGenerate` | — |
| `sd-cpp:getQueueStats` | — |
| `sd-cpp:searchHistory` | — |
| `sd-cpp:exportHistory` | — |
| `sd-cpp:edit` | — |

---

## Settings

_Source: `src/main/ipc/settings.ts`_

| Channel | Description |
|---------|-------------|
| `settings:get` | — |
| `settings:health` | — |
| `settings:save` | — |

---

## Theme

_Source: `src/main/ipc/theme.ts`_

| Channel | Description |
|---------|-------------|
| `theme:runtime:getAll` | — |
| `theme:runtime:install` | — |
| `theme:runtime:uninstall` | — |
| `theme:runtime:openDirectory` | — |
| `theme:getCurrent` | — |
| `theme:set` | — |
| `theme:getAll` | — |
| `theme:getDetails` | — |
| `theme:getCustom` | — |
| `theme:addCustom` | — |
| `theme:updateCustom` | — |
| `theme:deleteCustom` | — |
| `theme:toggleFavorite` | — |
| `theme:getFavorites` | — |
| `theme:isFavorite` | — |
| `theme:getHistory` | — |
| `theme:clearHistory` | — |
| `theme:getPresets` | — |
| `theme:applyPreset` | — |
| `theme:getCurrentPreset` | — |
| `theme:clearPreset` | — |
| `theme:export` | — |
| `theme:import` | — |
| `theme:duplicate` | — |

---

## Token Estimation

_Source: `src/main/ipc/token-estimation.ts`_

| Channel | Description |
|---------|-------------|
| `token-estimation:estimateMessage` | Registers IPC handlers for token estimation and context window management/ export function registerTokenEstimationIpc() {     const tokenEstimator = getTokenEstimationService();     const contextWindowService = getContextWindowService();      /**Estimate tokens for a single message |
| `token-estimation:estimateMessages` | Estimate tokens for multiple messages |
| `token-estimation:estimateString` | Estimate tokens for a string |
| `token-estimation:getContextWindowSize` | Get context window size for a model |
| `token-estimation:fitsInContextWindow` | Check if messages fit in context window |
| `context-window:getInfo` | Get context window information |
| `context-window:truncate` | Truncate messages to fit context window |
| `context-window:needsTruncation` | Check if truncation is needed |
| `context-window:getRecommendedSettings` | Get recommended truncation settings |

---

## Tools

_Source: `src/main/ipc/tools.ts`_

| Channel | Description |
|---------|-------------|
| `tools:execute` | — |
| `tools:kill` | — |
| `tools:getDefinitions` | — |

---

## Usage

_Source: `src/main/ipc/usage.ts`_

| Channel | Description |
|---------|-------------|
| `usage:checkLimit` | — |
| `usage:getUsageCount` | — |
| `usage:recordUsage` | — |

---

## User Behavior

_Source: `src/main/ipc/user-behavior.ts`_

| Channel | Description |
|---------|-------------|
| `user-behavior:get-model-recommendations` | Registers User Behavior IPC handlers.@param userBehaviorService The user behavior service instance./ export function registerUserBehaviorIpc(userBehaviorService: UserBehaviorService): void {     /**Gets model recommendations based on user behavior. |
| `user-behavior:get-recent-activity` | Gets recent user activity. |
| `user-behavior:get-frequent-features` | Gets frequently used features. |

---

## User Collaboration

_Source: `src/main/ipc/user-collaboration.ts`_

| Channel | Description |
|---------|-------------|
| `collaboration:sync:join` | registerUserCollaborationIpcRegisters IPC handlers for user-to-user real-time collaboration.Bridges collaboration events from the main process EventBus to the renderer./ export function registerUserCollaborationIpc(     mainWindowGetter: () => BrowserWindow \| null,     collaborationService: UserCollaborationService,     eventBus: EventBusService ): void {     const validateSender = createMainWindowSenderValidator(mainWindowGetter, 'user-collaboration operation');      // Helper:Forward specific event types to the renderer     const forwardToRenderer = (channel: string, payload: unknown) => {         const win = mainWindowGetter();         if (win && !win.isDestroyed()) {             win.webContents.send(channel, payload);         }     };      // Bridging collaboration events     eventBus.on('collaboration:joined', (payload) => forwardToRenderer('collaboration:sync:joined', payload));     eventBus.on('collaboration:left', (payload) => forwardToRenderer('collaboration:sync:left', payload));     eventBus.on('collaboration:sync', (payload) => forwardToRenderer('collaboration:sync:update', payload));     eventBus.on('collaboration:error', (payload) => forwardToRenderer('collaboration:sync:error', payload));      /**Join a collaborative room. |
| `collaboration:sync:send` | Send a synchronization update. |
| `collaboration:sync:leave` | Leave a collaborative room. |

---

## Window

_Source: `src/main/ipc/window.ts`_

| Channel | Description |
|---------|-------------|
| `window:openDetachedTerminal` | — |
| `shell:openExternal` | — |
| `shell:openTerminal` | — |
| `shell:runCommand` | — |
| `window:captureCookies` | AUD-2026-02-27-03: Rate limiter for shell:runCommand — 30 executions per minute */ const runCommandRateLimiter = new RateLimiter({     maxTokens: 30,     refillRate: 30,     refillIntervalMs: 60_000, });  interface DetachedTerminalWindowOptions {     sessionId: string;     title?: string;     shell?: string;     cwd?: string; }  /**Registers all window-related IPC handlers including window controls, shell operations, and cookies.@param getMainWindow - Factory function that returns the main BrowserWindow instance/ export function registerWindowIpc(getMainWindow: () => BrowserWindow \| null, allowedRoots: Set<string>) {     registerWindowControlHandlers(getMainWindow);     registerShellHandlers(getMainWindow, allowedRoots);     registerCookieHandlers(getMainWindow); }  function isPathAllowed(filePath: string, allowedRoots: Set<string>): boolean {     const absolutePath = path.resolve(filePath);     const isWin = process.platform === 'win32';     const normalizedPath = isWin ? absolutePath.toLowerCase() : absolutePath;     return Array.from(allowedRoots).some((root) => {         const resolvedRoot = path.resolve(root);         const normalizedRoot = isWin ? resolvedRoot.toLowerCase() : resolvedRoot;         const sep = isWin ? '\\' : path.sep;         return normalizedPath === normalizedRoot \|\| normalizedPath.startsWith(normalizedRoot.endsWith(sep) ? normalizedRoot : `${normalizedRoot}${sep}`);     }); }  function redactUrlForLogs(rawUrl: string): string {     try {         const parsed = new URL(rawUrl);         for (const key of parsed.searchParams.keys()) {             if (SENSITIVE_QUERY_KEYS.has(key.toLowerCase())) {                 parsed.searchParams.set(key, '[REDACTED]');             }         }         return parsed.toString();     } catch {         return rawUrl;     } }  function normalizeExecutableName(command: string): string {     const trimmed = command.trim();     const normalized = process.platform === 'win32' ? trimmed.toLowerCase() : trimmed;     const base = path.basename(normalized);     return base.replace(/\.(exe\|cmd\|bat)$/i, ''); }  /**AUD-SEC-034: Enforce HTTPS-only policy for cookie-capture URL allowlistOnly HTTPS is allowed for remote hosts; HTTP only for localhost/ function isCookieCaptureUrlAllowed(rawUrl: string): boolean {     try {         const parsed = new URL(rawUrl);         const host = parsed.hostname.toLowerCase();          // Check for localhost with HTTP or HTTPS         if (host === 'localhost' \|\| host === '127.0.0.1') {             return parsed.protocol === 'http:' \|\| parsed.protocol === 'https:';         }          // All other hosts must use HTTPS only         if (parsed.protocol !== 'https:') {             return false;         }          // Check against allowed hosts list         return COOKIE_CAPTURE_ALLOWED_HOSTS.has(host)             \|\| host.endsWith('.claude.ai')             \|\| host.endsWith('.anthropic.com')             \|\| host.endsWith('.github.com')             \|\| host.endsWith('.google.com');     } catch {         return false;     } }  /**Registers IPC handlers for window control operations (minimize, maximize, close, resize, fullscreen, detached terminal).@param getMainWindow - Factory function that returns the main BrowserWindow instance/ function registerWindowControlHandlers(getMainWindow: () => BrowserWindow \| null) {     const validateSender = createMainWindowSenderValidator(getMainWindow, 'window operation');      ipcMain.on('window:minimize', event => {         try {             const win = getMainWindow();             validateSender(event);             win?.minimize();         } catch {             /* ignore */         }     });      ipcMain.on('window:maximize', event => {         try {             const win = getMainWindow();             validateSender(event);             if (!win) {                 return;             }             if (win.isMaximized()) {                 win.unmaximize();             } else {                 win.maximize();             }         } catch {             /* ignore */         }     });      ipcMain.on('window:close', event => {         try {             const win = getMainWindow();             validateSender(event);             win?.close();         } catch {             /* ignore */         }     });      ipcMain.on('window:toggle-compact', (event, enabled) => {         try {             const win = getMainWindow();             validateSender(event);             if (!win) {                 return;             }             if (enabled) {                 win.setSize(COMPACT_WIDTH, COMPACT_HEIGHT);             } else {                 win.setSize(DEFAULT_WIDTH, DEFAULT_HEIGHT);             }         } catch {             /* ignore */         }     });      ipcMain.on('window:resize', (event, resolution: string) => {         try {             const win = getMainWindow();             validateSender(event);             if (!win) {                 return;             }             const [width, height] = resolution.split('x').map(Number);             if (width && height) {                 win.setSize(width, height);                 win.center();             }         } catch {             /* ignore */         }     });      ipcMain.on('window:toggle-fullscreen', event => {         try {             const win = getMainWindow();             validateSender(event);             if (!win) {                 return;             }             win.setFullScreen(!win.isFullScreen());         } catch {             /* ignore */         }     });      ipcMain.handle('window:openDetachedTerminal', async (event, optionsRaw: unknown) => {         validateSender(event);         const win = getMainWindow();         if (!win) {             return false;         }          const options = parseDetachedTerminalOptions(optionsRaw);         if (!options) {             return false;         }          const existing = detachedTerminalWindows.get(options.sessionId);         if (existing && !existing.isDestroyed()) {             if (existing.isMinimized()) {                 existing.restore();             }             existing.focus();             return true;         }          try {             const detachedWindow = new BrowserWindow({                 width: DETACHED_TERMINAL_WIDTH,                 height: DETACHED_TERMINAL_HEIGHT,                 minWidth: 640,                 minHeight: 260,                 show: false,                 autoHideMenuBar: true,                 backgroundColor: '#000000',                 title: options.title ? `${options.title} - Terminal` : 'Detached Terminal',                 webPreferences: {                     preload: path.join(__dirname, '../preload/preload.js'),                     sandbox: true,                     contextIsolation: true,                     nodeIntegration: false,                 },             });              const query = {                 detachedTerminal: '1',                 sessionId: options.sessionId,                 title: options.title ?? options.sessionId,                 shell: options.shell ?? '',                 cwd: options.cwd ?? '',             };              if (!app.isPackaged && process.env['ELECTRON_RENDERER_URL']) {                 const url = new URL(process.env['ELECTRON_RENDERER_URL']);                 Object.entries(query).forEach(([key, value]) => {                     url.searchParams.set(key, value);                 });                 await detachedWindow.loadURL(url.toString());             } else {                 await detachedWindow.loadFile(path.join(__dirname, '../renderer/index.html'), {                     query,                 });             }              detachedWindow.on('ready-to-show', () => {                 detachedWindow.show();                 detachedWindow.focus();             });              detachedWindow.on('closed', () => {                 detachedTerminalWindows.delete(options.sessionId);             });              detachedTerminalWindows.set(options.sessionId, detachedWindow);             return true;         } catch (error) {             appLogger.error(                 'WindowIPC',                 `Failed to open detached terminal: ${getErrorMessage(error)}`             );             return false;         }     }); }  /**Parses and validates raw input into detached terminal window options.@param value - Raw options object to parse@returns Validated options or null if invalid/ function parseDetachedTerminalOptions(value: unknown): DetachedTerminalWindowOptions \| null {     if (!value \|\| typeof value !== 'object') {         return null;     }      const raw = value as Record<string, unknown>;     const sessionId = typeof raw.sessionId === 'string' ? raw.sessionId.trim() : '';     if (!sessionId) {         return null;     }      const normalize = (input: unknown): string \| undefined => {         if (typeof input !== 'string') {             return undefined;         }         const trimmed = input.trim();         if (!trimmed) {             return undefined;         }         return trimmed.slice(0, MAX_TEXT_LENGTH);     };      return {         sessionId: sessionId.slice(0, MAX_TEXT_LENGTH),         title: normalize(raw.title),         shell: normalize(raw.shell),         cwd: normalize(raw.cwd),     }; }  /**Registers IPC handlers for shell operations (open external URLs, open terminal, run commands)./ function registerShellHandlers(getMainWindow: () => BrowserWindow \| null, allowedRoots: Set<string>) {     const validateSender = createMainWindowSenderValidator(getMainWindow, 'window operation');     ipcMain.handle('shell:openExternal', createValidatedIpcHandler('shell:openExternal', async (event, url: string) => {         validateSender(event);         appLogger.info('WindowIPC', `shell:openExternal handle called with URL: ${redactUrlForLogs(url)}`);          // Handle safe-file:// protocol for local images         if (url.startsWith('safe-file://')) {             const filePath = url.replace('safe-file://', '');             appLogger.info('WindowIPC', `Opening local file path: ${filePath}`);             try {                 const decodedPath = decodeURIComponent(filePath);                 if (!isPathAllowed(decodedPath, allowedRoots)) {                     return { success: false, error: 'Access denied' };                 }                 const error = await shell.openPath(decodedPath);                 if (error) {                     appLogger.error('WindowIPC', `shell.openPath failed: ${error}`);                     return { success: false, error };                 }                 return { success: true };             } catch (e) {                 appLogger.error('WindowIPC', `Safe file open catch: ${e}`);                 return { success: false, error: String(e) };             }         }          try {             const parsed = new URL(url);             const urlString = parsed.toString();              if (parsed.protocol === 'http:' \|\| parsed.protocol === 'https:') {                 appLogger.info('WindowIPC', `Opening URL with shell.openExternal: ${redactUrlForLogs(urlString)}`);                 try {                     await shell.openExternal(urlString);                     return { success: true };                 } catch (e) {                     appLogger.error(                         'WindowIPC',                         `shell.openExternal failed: ${getErrorMessage(e as Error)}`                     );                     return { success: false, error: String(e) };                 }             } else {                 return { success: false, error: 'Forbidden protocol' };             }         } catch (e) {             appLogger.error('WindowIPC', `openExternal catch: ${e}`);             return { success: false, error: String(e) };         }     }, {         argsSchema: z.tuple([urlSchema]),         defaultValue: { success: false, error: 'Validation failed' }     }));      ipcMain.handle('shell:openTerminal', createValidatedIpcHandler('shell:openTerminal', async (event, command: string) => {         validateSender(event);         if (process.platform === 'win32') {             // AUD-SEC-037: Hardened shell command validation             // Strict allowlist of safe characters only             const MAX_COMMAND_LENGTH = 1024;              if (command.length > MAX_COMMAND_LENGTH) {                 appLogger.warn('WindowIPC', 'Command exceeds maximum length');                 return false;             }              // Only allow alphanumeric, spaces, dashes, underscores, forward/back slashes, colons, and dots             // This is a very restrictive allowlist             const allowedPattern = /^[a-zA-Z0-9\s\-_./\\:]+$/;             if (!allowedPattern.test(command)) {                 appLogger.warn('WindowIPC', `Command contains forbidden characters: ${command}`);                 return false;             }              // Block dangerous patterns even if they pass the allowlist             const dangerousPatterns = [                 /\b(rm\|del\|format\|fdisk\|mkfs\|dd\|shutdown\|reboot\|halt\|poweroff)\b/i,                 /\b(reg\|registry\|regedit\|regsvr32)\b/i,                 /\b(net\|netsh\|ipconfig\|route\|arp)\b/i,                 /\b(taskkill\|tasklist\|wmic\|powershell\|pwsh\|cmd\|command)\b/i,                 /\b(curl\|wget\|nc\|netcat\|telnet\|ftp\|tftp)\b/i,                 /\b(python\|perl\|ruby\|node\|npm\|npx\|yarn\|pnpm)\b/i,                 /\b(git\s+push\|git\s+reset\|git\s+clean\|git\s+checkout)\b/i,             ];              for (const pattern of dangerousPatterns) {                 if (pattern.test(command)) {                     appLogger.warn('WindowIPC', `Command contains dangerous pattern: ${command}`);                     return false;                 }             }              spawn('cmd', ['/k', command], { shell: false });         } else {             // Basic fallback for Linux/Mac             appLogger.warn(                 'WindowIPC',                 `Open terminal not fully supported on non-windows yet: ${command}`             );         }         return true;     }, {         argsSchema: z.tuple([commandSchema]),         defaultValue: false     }));      ipcMain.handle('shell:runCommand', createValidatedIpcHandler('shell:runCommand', async (event, command: string, args: string[], cwd?: string) => {         validateSender(event);          // AUD-SEC-037: Hardened command validation         const MAX_COMMAND_LENGTH = 256;         const MAX_ARGS_COUNT = 50;         const MAX_ARG_LENGTH = 1024;          if (command.length > MAX_COMMAND_LENGTH) {             appLogger.warn('WindowIPC', 'Command exceeds maximum length');             return { stdout: '', stderr: 'Command too long', code: 1, error: 'Command validation failed' };         }          if (args.length > MAX_ARGS_COUNT) {             appLogger.warn('WindowIPC', 'Too many arguments');             return { stdout: '', stderr: 'Too many arguments', code: 1, error: 'Command validation failed' };         }          for (const arg of args) {             if (arg.length > MAX_ARG_LENGTH) {                 appLogger.warn('WindowIPC', 'Argument exceeds maximum length');                 return { stdout: '', stderr: 'Argument too long', code: 1, error: 'Command validation failed' };             }             if (/[\r\n\0]/.test(arg)) {                 appLogger.warn('WindowIPC', 'Argument contains forbidden control characters');                 return { stdout: '', stderr: 'Invalid argument', code: 1, error: 'Command validation failed' };             }         }          const executable = normalizeExecutableName(command);         if (!RUN_COMMAND_ALLOWED_EXECUTABLES.has(executable)) {             appLogger.warn('WindowIPC', `Blocked shell:runCommand executable: ${executable}`);             return { stdout: '', stderr: 'Executable is not allowed', code: 1, error: 'Command validation failed' };         }          if (cwd && !isPathAllowed(cwd, allowedRoots)) {             appLogger.warn('WindowIPC', `Blocked shell:runCommand cwd outside allowed roots: ${cwd}`);             return { stdout: '', stderr: 'Working directory is not allowed', code: 1, error: 'Command validation failed' };         }          // AUD-2026-02-27-03: Per-command argument validation (path traversal, injection)         const argPolicy = validateCommandArgs(executable, args);         if (!argPolicy.allowed) {             appLogger.warn('WindowIPC', `Blocked shell:runCommand args: ${argPolicy.reason}`);             return { stdout: '', stderr: argPolicy.reason ?? 'Argument policy violation', code: 1, error: 'Command validation failed' };         }          // AUD-2026-02-27-03: Rate limiting for execution attempts         if (!runCommandRateLimiter.tryAcquire()) {             appLogger.warn('WindowIPC', 'shell:runCommand rate limit exceeded');             return { stdout: '', stderr: 'Rate limit exceeded', code: 1, error: 'Rate limit exceeded' };         }          return new Promise(resolve => {             const resolvedCommand = resolveWindowsCommand(command);             appLogger.info('WindowIPC', `Running command: ${resolvedCommand} ${args.join(' ')}`);             const child = spawn(resolvedCommand, args, {                 cwd: cwd ?? process.cwd(),                 shell: false, // Disable shell for security             });              let stdout = '';             let stderr = '';              child.stdout.on('data', (data: Buffer) => {                 stdout += data.toString();             });              child.stderr.on('data', (data: Buffer) => {                 stderr += data.toString();             });              child.on('close', (code: number \| null) => {                 resolve({ stdout, stderr, code: code ?? 0, error: '' });             });              child.on('error', (err: Error) => {                 resolve({ stdout, stderr, code: 1, error: err.message });             });         });     }, {         argsSchema: z.tuple([commandSchema, z.array(z.string()), cwdSchema]),         defaultValue: { stdout: '', stderr: '', code: 1, error: 'Validation failed' }     })); }  /**Registers IPC handlers for cookie capture operations via hidden browser windows./ function registerCookieHandlers(getMainWindow: () => BrowserWindow \| null) {     const validateSender = createMainWindowSenderValidator(getMainWindow, 'window operation');     /**Opens a hidden BrowserWindow to capture cookies from a URL.Useful for capturing session cookies after OAuth completes in an external browser. |

---

## Workflow

_Source: `src/main/ipc/workflow.ts`_

| Channel | Description |
|---------|-------------|
| `workflow:getAll` | — |
| `workflow:get` | — |
| `workflow:create` | — |
| `workflow:update` | — |
| `workflow:delete` | — |
| `workflow:execute` | — |
| `workflow:triggerManual` | — |

---
